create table employees (
    id varchar(50) PRIMARY KEY,
    name varchar(50),
    surname varchar(50),
    email varchar(50)
);

create table salary (
   id varchar(50) PRIMARY KEY,
   amount integer
);
